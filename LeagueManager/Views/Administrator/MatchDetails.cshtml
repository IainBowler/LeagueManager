@model LeagueManager.Models.Match

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@{Html.RenderPartial("~/Views/Shared/_MatchDetails.cshtml", Model); }

<p>
    @Html.ActionLink("Edit", "EditMatch", new { id = Model.MatchId }) |
    @Html.ActionLink("Back to List", "ViewLeagueMatches", new { id = Model.League.Id })
</p>
